/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuración para Next.js - Usando proxy externo Python
  // El proxy en proxy_server.py maneja la comunicación con el backend
};

module.exports = nextConfig;
